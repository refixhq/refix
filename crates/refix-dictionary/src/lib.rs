//! The FIX dictionary model and the frontends that parse external formats
//! into it. Consumers (codegen, the runtime dialect descriptor, validation)
//! read the model and never touch the external formats directly.
